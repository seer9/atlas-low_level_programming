#include "main.h"

/**
 * main - print _putchar followed by a new line.
 * Return: 0
 */
int main(void)
{
	for (ole = 0; ole i < 1; ole++)
	{
		char *str = "_putchar\n";
		while(*str)
	{
		putchar(*str++);
	}
}
	return (0);
}
