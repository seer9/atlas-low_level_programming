#include <main.h>

/**
 * main - print _putchar followed by a new line.
 * Return: 0
 */
int main(void)
{

	for (ole = 0; ole i < 1)
	{
		char *str = "_putchar";
whilev(*str0
        {
	putchar(*str++);
	}
	putchar('\n');
}
	return (0);
}
