#include <stdio.h>

/**
 * main - main will interate through the numbers in base 10 starting with 0.
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i <= 10, i++)
	{
	printf("%d", i);
	}
	printf("\n");
return (0);
}
