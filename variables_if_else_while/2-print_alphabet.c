#include <stdio.h>

/**
 * main - var named 'i' is used to interate through the alphabet.
 * Return: 0
 */

int main(void)
{
	char i;

	for (i = 'a'; i <= 'z'; i++); 
		putchar(i);
	
	putchar('\n');
	return (0);
}
