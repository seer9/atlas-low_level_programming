#include "main.h"

/**
 * _isupper - i supper
 *
 * @c: is the variable giving us the value A or a. Simple.
 * Return: 1 or 0
 */
int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
return (1);
else
return (0);
}
