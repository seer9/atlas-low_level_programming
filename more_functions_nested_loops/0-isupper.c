#include "main.h"

/**
 * _isupper - i supper
 *
 * @c: is the variable giving us the value A or a. Simple.
 * Return: basically tricked it with or.
 */
int _isupper(int c)
{
if (c >= 'a' && c <= 'z')
return (0);
else
return (1);
}
